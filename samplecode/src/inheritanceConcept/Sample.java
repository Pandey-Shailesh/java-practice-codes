package inheritanceConcept;

public class Sample {

	/*Why multiple inheritance not support in the JAVA?
	 
	 ->Java doesn’t support Multiple inheritance using class because of “Diamond 
	   Ambiguity” problem.
	 -> Object is the Supermost class in JAVA 
	 -> To solve that we can remove the concept of default inheritance
	 ->All the userdefine super class are bidefaulted sub class in JAVA   
	 */
}
